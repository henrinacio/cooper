import type { Locale } from "@/lib/locale"

export const translations: Record<Locale, { logout: string }> = {
  en: { logout: "Logout" },
  pt: { logout: "Sair" },
  es: { logout: "Cerrar sesión" },
}
