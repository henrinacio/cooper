import type { Locale } from "@/lib/locale"

export const translations: Record<Locale, { renamedSuccess: string }> = {
  en: { renamedSuccess: "Module renamed" },
  pt: { renamedSuccess: "Módulo renomeado" },
  es: { renamedSuccess: "Módulo renombrado" },
}
